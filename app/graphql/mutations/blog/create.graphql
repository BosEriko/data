mutation createBlog($title: String!, $description: String!) {
  createBlog(input: { createBlogAttributes: { title: $title, description: $description } }) {
    errors
    blog {
      id
      title
      description
    }
  }
}
