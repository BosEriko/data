mutation SignUpMember($email: String!, $password: String!) {
  signUpMember(
    input: {
      signUpData: {
        credentials: {
          email: $email,
          password: $password
        }
      }
    }
  ) {
    id
    email
  }
}
