mutation SignInMember($email: String!, $password: String!) {
  signInMember(
    input: { credentials: { email: $email, password: $password } }
  ) {
    token
    member {
      id
      email
    }
  }
}
