mutation createPost($title: String!, $description: String!, $serverKey: String) {
  createPost(input: { createPostAttributes: { title: $title, description: $description, serverKey: $serverKey } }) {
    errors
    post {
      id
      title
      description
    }
  }
}
