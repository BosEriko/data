mutation createPost($title: String!, $description: String!) {
  createPost(input: { createPostAttributes: { title: $title, description: $description } }) {
    errors
    post {
      id
      title
      description
    }
  }
}
