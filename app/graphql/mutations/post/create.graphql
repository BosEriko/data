mutation createPost($title: String!, $content: String!) {
  createPost(input: { postAttributes: { title: $title, content: $content } }) {
    errors
    post {
      id
      title
      content
    }
  }
}
