mutation createPost($title: String!, $content: String!) {
  createPost(input: { createPostAttributes: { title: $title, content: $content } }) {
    errors
    post {
      id
      title
      content
    }
  }
}
