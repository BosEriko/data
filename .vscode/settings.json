{
  "editor.tabSize": 2,
  "editor.detectIndentation": false,
  "restoreTerminals.terminals": [
    {
      "splitTerminals": [
        {
          "name": "Rails Server",
          "commands": [
            "sudo service docker start",
            "sudo docker network prune -f",
            "sudo docker stop $(docker ps -aq)",
            "sudo docker rm $(docker ps -aq)",
            "cd $(eval echo $BOS_ERIKO_DATA_DIRECTORY) && docker-compose up"
          ]
        }
      ]
    },
    {
      "splitTerminals": [
        {
          "name": "Rails Console",
          "commands": ["cd $(eval echo $BOS_ERIKO_DATA_DIRECTORY) && docker-compose run web rails console"]
        }
      ]
    },
    {
      "splitTerminals": [
        {
          "name": "Terminal",
          "commands": ["cd $(eval echo $BOS_ERIKO_DATA_DIRECTORY) && clear"]
        }
      ]
    }
  ]
}